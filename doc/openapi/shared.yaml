
#
# Shared content between schemas.
#

components:
  schemas:
    id:
      type: string
    name:
      type: string
    statValue:
      type: integer
      minimum: 4
      maximum: 8
    Text:
      type: string
    ErrorModel:
      type: object
      properties:
        message: string
        source: string
        code: 
          type: integer
          format: int32
          minimum: 100
          maximum: 600
          default: 400
      required:
      - message
      - code

tags:
  - name: Dune RPG
    description: >
      Operation has something to do with Modiphius LLC RPG Dune: Adventures in the Impoerium.
    externalDocs: 
      url: "https://modiphius.net/pages/discover-dune-roleplaying-game"
  - name: Characters
    description: Operation targets characters.
  - name: Skills
    description: Operation targets skills.
  - name: Drives
    description: Operation targets drives.
  - name: Talents
    description: Operation targets talents.
  - name: Traits
    description: Operation targets traits.
  - name: Assets
    description: Operation targets assets. 
  - name: Update
    description: Operation updates one or more resource values.
  - name: Alter
    description: Operation alters properties of one or more resource values.
  - name: Remove
    description: Operation removes one or more resource values.
  - name: Create New
    description: Operation creates one or more resource values.
  - name: Get All
    description: Get all resource instances. 
  - name: Get One
    description: Get one resource instance.
  - name: Identifiers
    description: The operation acquires identifiers.
    